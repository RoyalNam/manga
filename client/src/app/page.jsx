import React from 'react';
import MainLayout from './(features)/layout';

const Home = () => {
    return (
        <MainLayout>
            <div>Home</div>
        </MainLayout>
    );
};

export default Home;
